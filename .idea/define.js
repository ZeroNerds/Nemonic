"use strict"

// Welcome
// =======
// here we attempt to work backwards from first principles for developing a peer shared 3D collaborative content space.
// feel free to contribute wherever there's a need; either by defining terms, or coding the definitions already provided. cheers!


// Define Sector
// =============
// a collction of quadrants defining a topic or theme.

function sector() {
  // ...
}

// Define Quadrant
// ===============
// quadrants are common zite lists, but not all peers of each quadrant will be hosting the zites themselves.
// there would be at least 1 copy of each zite hosted among all peers.
// as you move to a new quadrant your browser downloads whatever zite has lowest peers, but only if needed.

function quadrant(peers) {

  var quadrant_size = 0
  var low_peers = quadrant_size / peers

  if (file.peers =< low_peers) {
    peer.seed(file)
  }

}

// Define Zite
// ===========
// a small visual cloud of sites or apps represented by; screenshots, icons, titles, 3d objects, gifs (animations) etc.
// but not the content of the actual sites or apps.

function zite() {
  // ...
}

// Define Zone
// ===========
// public lists of peer connected hosted zites.

function zone() {
  // ...
}

// Define Peers
// ============
// list of members of a quadrant and any immediately neighbouring qaudrants.

function getPeers() {
  var peers = 0
  // quadrant(peers)
}

// Define Pack
// ===========
// a container archive of individual zites

function loadPack() {
  // ...
}

